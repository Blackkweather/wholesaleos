import { FollowUpQueue } from "./_follow-up-queue";

export const dynamic = "force-dynamic";
export const metadata = { title: "Follow-Ups" };

export default function FollowUpsPage() {
  return <FollowUpQueue />;
}
